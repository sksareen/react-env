// Test 1: Minimal JSX
// This tests the absolute minimum JSX needed to render
// No hooks, no functions, just a static component

function BasicJSX() {
  return (
    <div>
      <h2>Basic JSX Test</h2>
      <p>This is a static component with no hooks or event handlers.</p>
    </div>
  );
}

export default BasicJSX;